# 기능 상세 설명

## 1. 일정 생성 (메인 페이지)

### 경로
- `/` (홈페이지)

### 기능
- **모임 제목 입력**: 기본값 "걸뱅이크루 모임"
- **날짜 선택**: 오늘 날짜 자동 설정
- **시간대 관리**:
  - 기본 시간대 3개 제공 (오전 10시, 오후 2시, 오후 6시)
  - 시간대 추가/삭제 가능
  - Enter 키로 빠른 추가
- **멤버 관리**:
  - 기본 멤버 3명 제공
  - 멤버 추가/삭제 가능
  - Enter 키로 빠른 추가

### 유효성 검사
- 시간대 최소 1개 이상 필수
- 멤버 최소 1명 이상 필수

### 동작
- "일정 만들기" 버튼 클릭 시:
  1. 고유 ID 생성 (10자리 nanoid)
  2. LocalStorage에 일정 저장
  3. 투표 페이지로 자동 이동

## 2. 투표 페이지

### 경로
- `/vote/[id]` (공유용 링크)

### 기능
- **멤버 선택**:
  - 2열 그리드로 표시
  - 이미 투표한 멤버는 체크마크(✓) 표시
  - 선택한 멤버 강조 표시 (보라색)

- **시간대 선택**:
  - 복수 선택 가능
  - 선택한 시간대 체크마크(✓) 표시
  - 선택한 시간대 강조 표시 (파란색)

- **투표 제출**:
  - 멤버와 시간대 필수 선택
  - 투표 완료 시 LocalStorage에 기록
  - 자동으로 결과 페이지로 이동

- **투표 수정**:
  - 이미 투표한 멤버 선택 시 기존 투표 내용 로드
  - 수정 모드 활성화 알림
  - 수정 후 재제출 가능

- **링크 공유**:
  - 현재 페이지 URL 복사 버튼
  - 클립보드에 자동 복사

### 중복 방지
- LocalStorage 기반 투표 기록 저장
- 키 형식: `voted_${scheduleId}_${memberId}`
- 수정은 가능하지만 중복 투표는 불가

## 3. 결과 페이지

### 경로
- `/result/[id]`

### 기능

#### 시간대별 참여 현황
- **프로그레스 바**: 참여율 시각화
- **최다 선택**: 가장 많은 투표를 받은 시간대 강조 (초록색)
- **참여자 리스트**: 각 시간대별 투표한 멤버 표시
- **참여 인원**: 각 시간대별 투표 수 표시

#### 멤버별 참여 현황
- **투표 완료**: 초록색 배경, "투표 완료" 뱃지
- **미참여**: 회색 배경, "미참여" 뱃지
- **선택 시간대**: 각 멤버가 선택한 모든 시간대 표시

#### 액션
- **투표하기/수정하기**: 투표 페이지로 이동
- **새 일정 만들기**: 메인 페이지로 이동
- **링크 공유**: 투표 링크 복사

### 통계
- 총 참여 인원 표시
- 실시간 업데이트 (페이지 새로고침 시)

## 4. 카카오톡 공유

### Open Graph 태그
- **제목**: 걸뱅이크루 일정 관리
- **설명**: 모임 일정을 쉽게 조율하세요
- **이미지**: 1200x630 SVG 이미지
- **로케일**: ko_KR

### 동적 OG 이미지
- `/vote/[id]/opengraph-image`
- Next.js ImageResponse API 사용
- 그라데이션 배경 (보라색 계열)

## 5. 데이터 저장

### LocalStorage 구조
```javascript
{
  "schedules": {
    "[scheduleId]": {
      "id": "10자리 ID",
      "title": "모임 제목",
      "date": "YYYY-MM-DD",
      "timeSlots": [...],
      "members": [...],
      "votes": [...],
      "createdAt": "ISO 날짜"
    }
  }
}
```

### 투표 기록
- 키: `voted_${scheduleId}_${memberId}`
- 값: `"true"`

## 6. 모바일 최적화

### 반응형 디자인
- 모든 페이지 모바일 우선 설계
- Tailwind CSS 유틸리티 클래스 활용
- Touch-friendly 버튼 크기

### Viewport 설정
- width: device-width
- initial-scale: 1
- maximum-scale: 1 (확대 방지)
- user-scalable: false

### 성능 최적화
- Next.js 15 + Turbopack
- 자동 코드 스플리팅
- 정적 페이지 프리렌더링

## 7. UI/UX 특징

### 디자인 원칙
- **직관성**: 모든 기능이 한눈에 파악 가능
- **피드백**: 모든 액션에 즉각적인 시각적 피드백
- **일관성**: 전체 페이지 일관된 디자인 언어
- **접근성**: 명확한 레이블과 충분한 대비

### 색상 시스템
- **Primary**: 파란색 계열 (시간대)
- **Secondary**: 보라색 계열 (멤버)
- **Success**: 초록색 (완료/최다)
- **Warning**: 주황색 (수정 모드)
- **Error**: 빨간색 (삭제)

### 인터랙션
- 호버 효과: 모든 클릭 가능 요소
- 트랜지션: 부드러운 색상 전환
- 그림자: 깊이감 표현
- 그라데이션: 주요 CTA 버튼

## 8. 향후 개선 가능 사항

### 백엔드 연동
- 실시간 동기화 (WebSocket/Server-Sent Events)
- 데이터베이스 저장 (MongoDB, PostgreSQL)
- 사용자 인증 (소셜 로그인)

### 추가 기능
- 푸시 알림
- 캘린더 동기화
- 투표 마감 시간 설정
- 댓글 기능
- 투표 결과 엑셀 내보내기
- 일정 삭제/수정 기능 (주최자 전용)

### 분석
- Google Analytics 연동
- 사용 패턴 분석
- A/B 테스트

### 국제화
- 다국어 지원 (i18n)
- 시간대 자동 변환
